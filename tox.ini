[tox]
envlist = black, compileall

[testenv]
skip_install = true
deps = -r requirements-dev.txt

[testenv:black]
commands = black --check --diff .

[testenv:compileall]
commands = python -m compileall -q main.py abs_api.py service.py tools/

# No mypy env, deliberately. Run strict over main.py/abs_api.py/service.py and
# it reports ~104 errors, almost all missing annotations across main.py's 1200
# lines. A config relaxed enough to pass today would check nothing, and
# annotating the addon is a change to its behaviour surface, not a CI change —
# so it is not smuggled in under a CI commit.
#
# To add it later, in its own PR: annotate abs_api.py first (it is the smallest
# and the only module with a testable shape), add mypy.ini with `files = abs_api.py`,
# then widen module by module. script.kofin.lyrics' mypy.ini is the pattern.
