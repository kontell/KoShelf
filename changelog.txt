v1.0.0
- Koshelf is now Kotome. The name collided with an unrelated project.
- The add-on id changed to plugin.audio.kotome, so Kodi installs this as a
  new add-on rather than an update. Your server address, sign-in and
  per-book speeds are carried over automatically the first time it runs.
  Once you have checked it worked, the old Koshelf entry can be uninstalled;
  its data is left alone rather than deleted.
- Skin properties are published under both Kotome.* and Koshelf.* names for
  the whole 1.x line, so anything keyed on the old names keeps working.
- Nothing else changed in this release.

v0.24.0
- Your password is no longer stored. Sign-in is a button in Settings that
  asks for the password once, exchanges it for tokens, and forgets it. An
  existing install is migrated automatically on first run — you stay signed
  in, and the stored plaintext password is cleared.
- Uses AudioBookShelf's current token model: a one-hour access token plus a
  refresh token, refreshed automatically when it expires and again before
  playback starts. The old login read user.token, which the server source
  marks "Old non-expiring token" — it still works and is still accepted from
  older servers, but nothing new is issued on it.
- Settings now shows which server and account you are signed in to, with
  Test connection and Sign out buttons.
- The server address accepts a bare host name (http, port 13378), host:port,
  or a full URL.
- Signing in checks the address before asking for a password, so a typo
  reads as "could not reach that server" rather than a failed login.
- New: Verify HTTPS certificate, for a server behind a self-signed one.
- Each install now identifies itself to the server with its own device ID
  instead of a constant shared by every copy of the add-on.
- Books and Continue Listening have their own icons.

v0.23.0
- The sort menu now works. Picking "Recently added" returned the right page
  from the server and then Kodi re-sorted it alphabetically, because the
  listing also registered SORT_METHOD_TITLE — Container.SortMethod read
  "Title" while the sort menu said otherwise. Server-sorted listings now
  register only SORT_METHOD_UNSORTED, so the server's order is what you see.
- The sort picker moved from a row in the list to the context menu, where it
  is available on every item and every page instead of only at the top of
  page one. The current sort is shown in the breadcrumb.
- More sorts: author Z-A, narrator, series, oldest added, published year
  ascending, and size.
- In-progress books and episodes now carry a real Kodi resume point instead
  of "[42%]" in the title. Skins draw their own in-progress indicator from
  it, Kodi offers Resume / Start from beginning, and finished items are
  marked watched. The percentage text is gone, and with it the reason
  Continue Listening could not be sorted by title.
- Narrator, duration, genre and year moved out of the label and into the
  info tag, where the skin can lay them out — in Contuary the label markup
  was not rendered at all, while the duration as a tag gets its own column.
- Every folder now has an icon, using Kodi's own Default*.png names so they
  match whatever skin you run. Sort, Next page and Log in use bundled
  Material Symbols, which is all Kodi has no icon for.
- Menu listings use an empty content type. With "files" the skin drew its own
  folder icon over the one the item carried, so every row looked identical.
- Books and podcasts without a cover on the server now show the bundled
  placeholder instead of a broken image.
- Added fan art.
- Removed the Music player (PAPlayer) option. It was not supported.
- Fix: a book whose published year is a full date ("2024-12-17" is a real
  value from ABS metadata) crashed the whole listing.

v0.22.0
- Browsing is roughly five times faster. Every listing used to spend
  0.8-1.6 s starting up before it did any work at all, most of it
  "import requests" — measured on a desktop; an ARM box pays a multiple.
  Root menu 0.91 s -> 0.18 s, Authors 1.08 s -> 0.09 s.
- requests is replaced by a small standard-library HTTP transport. A plugin
  invocation makes one to three calls and exits, so requests' connection
  pooling never repaid its import cost. The script.module.requests
  dependency is gone.
- Interpreter reuse (reuselanguageinvoker) is on, which takes the startup
  cost to zero on repeat invocations. Note that add-on code changes now
  need a disable/enable bounce to take effect.
- The cached token is no longer checked with a throwaway API call on every
  single listing. An expired token is handled where it shows up instead.
- The progress map (/api/me, ~59 KB) and the API client are cached between
  invocations with a short TTL, so a second folder in a row is nearly free.
  Responses are now gzipped.

v0.21.0
- Fix: the sleep timer could freeze Kodi outright. The full-screen overlay
  it drew was an xbmcgui.WindowDialog that _expire() deliberately left open;
  it outlived the service, survived the add-on being disabled, and once
  ownerless the next Back press deadlocked Kodi's application thread inside
  CScriptInvocationManager::Process(). Nothing rendered, no window would
  activate, Application.Quit returned OK and did nothing, and SIGTERM was
  ignored — only SIGKILL recovered it. The overlay is gone.
- The sleep timer now uses Kodi's own screensaver instead of drawing its
  own. It borrows screensaver.mode (an empty mode draws nothing) and
  screensaver.disableforaudio (true by default, and it makes
  ActivateScreenSaver a silent no-op during audio playback), and puts both
  back when you come back. On expiry the screen is left dark and the
  screensaver re-armed, because stopping playback drops it.
- Removed: Screen off (HDMI-CEC). It needs peripheral.libcec, which does
  not exist on Android and is not installed on a stock desktop Kodi; it
  did nothing on either machine it was tested on.
- Renamed: Screen off (Android / Google TV) is now "Turn the display off
  (Linux/X11 only)", because that is what it is. Kodi has no DPMS
  implementation on Android — the builtin returns without acting and
  without logging. On Android the screensaver is used instead.
- New: "Screensaver: my usual one", which activates whatever screensaver
  you already have configured rather than forcing black or dim.
- Fix: the "Idle seconds before screen action" setting used operator
  "isnot", which Kodi does not have, so the whole dependency was discarded
  and the setting showed even with the screen action set to None.
- Fix: opening Settings from the add-on menu left its directory handle
  open for as long as the dialog was up, and an unrecognised route never
  closed one at all. Both now close, so a favourite or library node
  pointing at them cannot hang.
- Fix: a trailing slash on a plugin URL lands on the last query parameter,
  not on "action". ?action=library&...&media_type=book/ matched neither
  branch and returned an empty folder with no error.

v0.20.0
- Sleep timer screen options restructured: single "Screen action" setting
  with five modes — Screensaver: Black, Screensaver: Dim, Screen off
  (HDMI-CEC), Screen off (Android / Google TV), or None. Replaces the
  previous dim toggle + screensaver picker + display-off checkbox.
- Fix: screensaver no longer pulsates. The screen action now fires once
  per idle period and only re-arms when the user interacts, instead of
  repeatedly calling ActivateScreenSaver every 250 ms.
- Fix: sleep_last_preset now has the required <control> tag for Kodi v22
  (was causing repeated parse errors in the log).
- Custom sleep timer shows a numeric keypad instead of a full text keyboard.
- If a custom duration was previously used, it appears in the preset list
  with "(last used)" and is preselected.
- Fix: widget URLs with a trailing slash (e.g.
  plugin://plugin.audio.koshelf/?action=continue_listening/) now route
  correctly.
- Remove the "Comment:" field from book and podcast episode info dialogs.
  Descriptions are still shown via VideoPlayer's Plot field during playback.
- Podcast directory items now have MusicInfoTag (title, artist, genres) and
  cover art — the i (info) button works.
- Author items now have MusicInfoTag (artist name, bio) — the i (info)
  button works.

v0.19.0
- Sleep timer with screen-dim and volume ramp-down. While playing, the
  "Sleep timer" row in the Koshelf root menu opens a dialog with
  presets (5/10/15/30/45/60/90 min), End of chapter, Custom..., and
  Cancel timer. The last-used preset is highlighted on next visit.
- While a sleep timer is active, Kodi's screensaver is temporarily
  swapped for a black or dim mode that activates after a shorter idle
  period (Settings → Sleep timer). The original screensaver is
  restored when the timer expires or is cancelled. Crash-recovery
  restores it on next service start if Kodi was killed mid-timer.
- Volume fades to zero over the last N seconds before the timer fires
  (default 30, configurable, 0 to disable). Adjusting the volume
  manually during the ramp cancels it for the rest of the timer and
  doesn't restore on cancel.
- Optional: send a CEC standby on expiry to power off the connected
  TV (HDMI-CEC required). Off by default.

v0.18.1
- Restore audio-only categorisation. The addon now shows only under
  Music add-ons, not Video add-ons; the v0.18.0 dual categorisation
  caused the i (info) button to no-op when the addon was launched from
  Video add-ons (DialogVideoInfo never opened).
- VideoPlayer playback is still the default. Player core selection is
  driven by the ListItem's VideoInfoTag, not by the addon's <provides>
  category, so removing "video" from <provides> doesn't affect which
  player core handles playback.

v0.18.0
- Video player is the default again. With inputstream.tempo 0.3.10's
  dynamic ptsStart (computed from popped-packet pts/dispTime), the
  Video player OSD now tracks content time at non-1× tempo instead of
  ticking at wall-clock — the regression that caused v0.14.0 to drop
  the video path. The Video player also brings the richer now-playing
  UI (cover-art and visualisation fanart) and surfaces embedded source
  chapters via the music OSD's Previous/Next chapter buttons.
- Music player is still available as an opt-in under
  Settings → Playback → Player. PAPlayer's lighter pipeline is kept
  for users who prefer it; resume, tempo, position sync, and per-book
  speed tracking work in either mode. <provides> now includes "video"
  in addition to "audio".
- Resume works the same in both modes. The Video player branch sets
  StartOffset (ms) plus ResumeTime/TotalTime; the Music player branch
  keeps audiobook_bookmark. Both set inputstream.tempo.start_time so
  the addon's player-agnostic seek-hold prevents start-of-file audio
  leaks before the resume seek lands.
- Requires inputstream.tempo 0.3.10 or newer.

v0.17.2
- Fix: a failed replay from Continue Listening was erasing the book's
  resume position. When PAPlayer couldn't open the stream (e.g.
  transient HTTP error), the service would run its close-session path
  with last_time=0 and sync that back to ABS, clobbering the bookmark.
  Now the close path skips sync_session when last_time < 5 s while
  start_time > 30 s — playback obviously never resumed, so don't
  pretend the user listened to the first 0 s.

v0.17.1
- Move the [NN%] progress indicator to after the title in library
  listings (All Books, Series detail, Author books, Collections,
  Podcast Episodes, Recent Episodes). Leading '[' was sorting before
  A-Z in Kodi's title sort, clustering in-progress items at the top
  regardless of name. Continue Listening still shows the indicator
  as a prefix — that view is ordered by last-played, not title.
- Sanitise descriptions before they reach Kodi's Info dialog. ABS
  often imports descriptions with HTML markup (<p>, <b>, <i>, &amp;,
  &nbsp; etc.) copied from Audible; Kodi was rendering the tags
  verbatim. Now parsed to plain text with paragraph breaks preserved,
  HTML entities decoded, and NBSP normalised to ASCII space.

v0.17.0
- Books and podcast episodes in library listings now show a
  "[NN%]" prefix when in progress — same format Continue Listening
  uses, so an item looks the same whether you reach it via resume
  or by browsing. Covers All Books, Series, Authors, Collections,
  Podcast Episodes, and Recent Episodes.

v0.16.0
- Resume no longer leaks a fraction of a second from the start of the
  book. On resume, Koshelf sets inputstream.tempo.start_time in addition
  to the usual audiobook_bookmark property. inputstream.tempo 0.3.6+
  uses that hint to hold packet output until PAPlayer's bookmark seek
  arrives (or a 2 s safety timeout) — so no pts=0 audio reaches the
  sink before the seek lands. Requires inputstream.tempo 0.3.6.

v0.15.0
- Library listings now sort server-side via ABS, so the order is
  consistent across pages instead of only within the loaded page.
  A "[ Sort: ... ]" item at the top opens a picker (Title, Author,
  Recently added/updated, Duration, Published year, Random). The
  choice is preserved through pagination.
- Background art: listing items expose their cover as fanart so
  skins with a blurred-fanart visualization (Estuary and similar)
  use the book/podcast cover as a backdrop during playback.
- Info dialog: books in Continue Listening now include the book
  description, and every playable item reports mediaType='song' so
  most skins show the full music Info dialog with description.

v0.14.0
- Removed the Player setting and the Video player path. PAPlayer is now
  the only player. The video path was originally added to chase a
  tempo-tracking OSD, but its state.time progressed at wall-clock rather
  than tempo-adjusted rate, so it offered no benefit over the music
  player path.

v0.13.0
- New Player setting: choose between the Music player (PAPlayer, default)
  and the Video player (VideoPlayer). Tempo, resume, position sync, and
  per-book speed tracking all work in both modes. The video player path
  uses VideoPlayer's StartOffset for resume plus a service-side
  onAVStarted fallback. The addon now declares <provides>audio video</provides>.
- Requires inputstream.tempo 0.3.1 or newer so the OSD position tracks
  content time at non-1× tempo.

v0.12.3
- Resume playback uses PAPlayer's native audiobook_bookmark property.
  PAPlayer seeks before audio output begins — no race conditions, no
  Python seek hack, no C++ intercept.
- Rename KoShelf → Koshelf throughout.
- Configurable items per page (25–500, default 100) for full client-side
  sorting of large libraries.
- Episode title shown on OSD (uses session.displayTitle); album set to
  podcast name for episodes.
- Description available in info dialog (press i) via setComment() in all
  browsing routes and during playback.
- Now playing root label: speed at front, always two decimal places.
- Max speed lower bound changed from 1.5x → 1.0x (allows disabling tempo).
- Speed step description updated to reference PgUp/PgDn keys.
- Sync guard prevents overwriting a valid resume position with 0.

v0.10.5
- Service-triggered root refresh when tempo sentinel appears/disappears.
- Move Playback section to first in settings.
- Tighten service loop to 0.25s for faster resume-seek latency.

v0.10.4
- Continue Listening default sort uses SORT_METHOD_NONE (server returns
  items in last-played-descending order) and exposes LastPlayed, Title,
  Artist, Duration as alternatives.
- Progress percentage moved into the InfoTag title so it survives album-
  style views that only show the tag title.
- Add artist field to podcast items.
- Cache root directory is invalidated (cacheToDisc=False) so the "Now
  playing" row reappears on next root visit.

v0.10.3
- Drop setDateAdded from InfoTagMusic (it doesn't exist — only VideoTag has
  one) and SORT_METHOD_DATEADDED from book sorts. Was crashing every
  listing after the v0.10.0 sort/date additions.

v0.10.2
- Show a "Now playing: <title> — <speed>" item at the root when tempo is
  the active inputstream; selecting it opens a speed picker.

v0.10.1
- Fix settings rendering for Kodi 21: sliders use <format>{:.2f}x</format>
  inline instead of the formatlabel-with-id pattern, speed_step uses
  type=string with option-label string IDs, and empty-default fields
  declare <allowempty>true</allowempty>.

v0.10.0
- Add sort-order options to every browsing view (title, artist, album,
  duration for books; last played/title/artist/duration for Continue
  Listening; date/title/duration for podcast episodes).
- Continue Listening defaults to last-played descending.
- Drop API key auth; username + password only (cached session token
  handled transparently).
- Bind Up/Down to BigStepForward/BigStepBack in audio contexts for
  progressive skip. (Moved out to inputstream.tempo's keymap in later
  versions.)

v0.9.1
- Migrate settings.xml to Kodi 21's structured format with help text,
  slider controls for speed ranges, and a Playback/General section split.
- Speed step (spinner) + min/max/book/podcast speed sliders formatted
  as "1.50x".
- Clamp saved per-book speeds against the current min/max at replay.
- Share step/min/max with inputstream.tempo via a JSON file in
  special://temp/inputstream_tempo_config.
- Drop the speed_mode setting — always step by speed_step now.
- Skip ebook-only library items in both Continue Listening and regular
  library listings.

v0.9.0
- Initial release through GitHub Actions.
